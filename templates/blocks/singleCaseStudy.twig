{% extends 'blocks/__base__' %}

{% set classes = ['block-wrapper--no-padding', 'group', 'lg:min-h-[300px]'] %}
{% set caseStudy = item.caseStudy.one() %}
{% set bgImage = caseStudy.defaultImage.one() %}

{% block block_under %}
    {% if bgImage %}
        {% include 'components/background-image' with {
            image: bgImage,
            bg: {
                fixed: true
            },
            overlay: {
                blur: true
            }
        } %}
    {% endif %}
{% endblock %}

{% block block_outer %}
    <h2 class="gs-lazy">
        <a href="{{ caseStudy.url }}"
            title="View Project: {{ caseStudy.title }}"
            rel="noopener"
            class="md:group-hover:scale-120 lg:group-hover:scale-110 transition-all duration-400 ease-in-out block block-wrapper text-center">
            <span class="block block-container">{{ caseStudy.title }}</span>
        </a>
    </h2>
{% endblock %}
